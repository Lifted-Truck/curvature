# Implausible objects — material signifiers physics forbids

Coherence (checkpoint-0 claim 2) is what keeps these reading as
*objects* even though no such object can exist.

- `morph_gamelan_to_string.wav` — struck 4x while its metric morphs from clustered-inharmonic to exact harmonic series: one object *becoming a different instrument* mid-phrase
- `bell_anti_acoustic.wav` — negative damping tilt: lows die first, shimmer outlasts the thump — decay structure of a time-reversed material
- `rubber_bell.wav` — bronze attack, balloon behavior: the object's size oscillates during ring-out and the whole spectrum breathes as one
- `comb_damped_genus2.wav` — a substance that absorbs every other mode: interleaved long-ring and near-mute partials — selective absorption no homogeneous material has
